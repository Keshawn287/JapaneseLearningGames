import { EnterTabDirective } from './enter-tab.directive';

describe('EnterTabDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterTabDirective();
    expect(directive).toBeTruthy();
  });
});
