import { EnableControlDirective } from './enable-control.directive';

describe('EnableControlDirective', () => {
  it('should create an instance', () => {
    const directive = new EnableControlDirective();
    expect(directive).toBeTruthy();
  });
});
